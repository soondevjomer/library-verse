spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_verse
    username: root
    password: "#Akosijomer02"
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true
        format_sql: true
logging:
  level:
    org.springframework: info